{
  "skill_name": "orocommerce-skills",
  "evals": [
    {
      "id": 1,
      "eval_name": "oro-bundle-1",
      "skill": "oro-bundle",
      "prompt": "I need to create a new OroCommerce bundle called AcmeInventoryBundle that will handle custom warehouse logic. Set up the full bundle skeleton with DI extension, services.yml, bundles.yml registration, and a navigation entry under the 'Inventory' menu in the back-office sidebar. Also add a system configuration option for 'default_warehouse_code' as a string field.",
      "assertions": [
        "Bundle class with DI Extension using #[\\Override] attribute",
        "bundles.yml registration with priority 0 or no priority specified",
        "services.yml and navigation.yml with menu item config under system_tab_panel",
        "system_configuration.yml with default_warehouse_code field definition and messages.en.yml with translation keys"
      ],
      "files": []
    },
    {
      "id": 2,
      "eval_name": "oro-bundle-2",
      "skill": "oro-bundle",
      "prompt": "I need to decorate the existing oro_product.handler.create service with my own handler that adds automatic SKU generation before product creation. Show me how to set up the service decoration in my AcmeProductBundle.",
      "assertions": [
        "services.yml using 'decorates' key targeting oro_product.handler.create",
        "Inner service injected via constructor argument",
        "PHP decorator class that wraps and delegates to the original handler"
      ],
      "files": []
    },
    {
      "id": 3,
      "eval_name": "oro-entity-3",
      "skill": "oro-entity",
      "prompt": "Create a new 'WarehouseLocation' entity in our AcmeInventoryBundle. It should have: name (string, required), code (string, unique), capacity (integer), isActive (boolean, default true), and a ManyToOne relation to Organization. The entity needs USER ownership so warehouse managers can only see their own locations. Write the entity class and the migration.",
      "assertions": [
        "Entity uses PHP 8 ORM attributes (not annotations) with #[Config] including ownership set to USER and mandatory organization_field_name",
        "Entity implements ExtendEntityInterface and uses ExtendEntityTrait",
        "Table name follows Oro convention (e.g. acme_warehouse_location)",
        "Migration class with createTable defining all columns and foreign keys"
      ],
      "files": []
    },
    {
      "id": 4,
      "eval_name": "oro-entity-4",
      "skill": "oro-entity",
      "prompt": "I need to add a custom 'warehouse_notes' text field to the existing OroCommerce Product entity via my AcmeInventoryBundle. Write the migration that extends the Product entity.",
      "assertions": [
        "Migration implements ExtendExtensionAwareInterface with extendExtension property",
        "Uses $this->extendExtension->addColumn() on 'oro_product' table",
        "Column options include ExtendScope::OWNER_CUSTOM in extend/owner"
      ],
      "files": []
    },
    {
      "id": 5,
      "eval_name": "oro-datagrid-5",
      "skill": "oro-datagrid",
      "prompt": "Create a datagrid for our WarehouseLocation entity that shows name, code, capacity, isActive, and the owner's username. Include filters for name (string), isActive (boolean), and capacity (number range). Add view/edit/delete row actions and a mass delete action. The grid should support inline editing for the 'capacity' and 'isActive' fields.",
      "assertions": [
        "datagrids.yml with ORM source query joining the owner relation",
        "Columns with correct frontend_type values, sorters with proper data_name aliases, and filters for name/isActive/capacity",
        "Row actions (view/edit/delete) with link and icon config, plus mass_actions with acl_resource",
        "inline_editing enabled on capacity and isActive columns"
      ],
      "files": []
    },
    {
      "id": 6,
      "eval_name": "oro-datagrid-6",
      "skill": "oro-datagrid",
      "prompt": "I need to add a 'warehouse_code' column to the existing products-grid in OroCommerce. The warehouse code comes from a related WarehouseLocation entity via product.warehouseLocation.code. Use an event listener approach since I don't want to modify the core grid config.",
      "assertions": [
        "Event listener class with onBuildBefore method to add column/filter config dynamically",
        "Service registration with oro_datagrid.event.build_before tag scoped to the products-grid",
        "Join and column definition added programmatically via DatagridConfiguration"
      ],
      "files": []
    },
    {
      "id": 7,
      "eval_name": "oro-api-7",
      "skill": "oro-api",
      "prompt": "Expose our WarehouseLocation entity via the back-office REST API. Include all fields except 'internalNotes', rename 'isActive' to 'active' in the API output, add string filters for name and code, and disable the delete_list action. Also expose it on the storefront API but only with name, code, and active fields (read-only, no create/update/delete).",
      "assertions": [
        "api.yml with entity config: field exclusion for internalNotes, property_path rename for isActive->active, and filters for name/code",
        "api.yml disables delete_list action",
        "api_frontend.yml with separate config using exclusion_policy 'all' and only specified fields included",
        "api_frontend.yml disables all write actions (create, update, delete, delete_list)"
      ],
      "files": []
    },
    {
      "id": 8,
      "eval_name": "oro-api-8",
      "skill": "oro-api",
      "prompt": "I need to create a custom API processor that automatically sets the 'lastCheckedAt' timestamp on WarehouseLocation entities whenever they're updated via the API. Hook into the right processor group.",
      "assertions": [
        "Processor class registered as a service with oro.api.processor tag",
        "Processor targets customize_form_data or normalize_data group for the update action",
        "Processor scoped to WarehouseLocation entity class via context check or tag attribute"
      ],
      "files": []
    },
    {
      "id": 9,
      "eval_name": "oro-workflow-9",
      "skill": "oro-workflow",
      "prompt": "Create a warehouse inspection approval workflow for our WarehouseLocation entity. Steps: 'pending_inspection' -> 'under_review' -> 'approved' or 'failed'. The 'approve' transition should only be available to users with the EDIT permission. Add an automatic transition that moves to 'failed' if the inspection isn't completed within the current step for more than 30 days. Include a transition form on 'submit_review' that collects inspector_notes (textarea).",
      "assertions": [
        "workflows.yml with steps (pending_inspection, under_review, approved, failed) and transitions between them",
        "ACL precondition on approve transition checking EDIT permission",
        "Transition form on submit_review collecting inspector_notes attribute as textarea",
        "Timed/scheduled transition pattern for 30-day auto-fail"
      ],
      "files": []
    },
    {
      "id": 10,
      "eval_name": "oro-workflow-10",
      "skill": "oro-workflow",
      "prompt": "I need to customize the OroCommerce checkout workflow to add a 'warehouse selection' step before the shipping step. The customer should pick from available warehouses. Show me how to import the base checkout workflow and inject my custom step.",
      "assertions": [
        "Workflow config that imports b2b_flow_checkout as base",
        "Custom warehouse_selection step inserted before the shipping step",
        "is_checkout_workflow metadata preserved in the extended workflow"
      ],
      "files": []
    },
    {
      "id": 11,
      "eval_name": "oro-frontend-11",
      "skill": "oro-frontend",
      "prompt": "Create a custom storefront theme called 'acme-warehouse' that inherits from the default theme. It should have a custom color palette (primary: #1a5276, secondary: #f39c12), a custom logo, and override the product listing page to show a 'warehouse availability' badge next to each product name. Set up the full theme structure with SCSS and layout updates.",
      "assertions": [
        "theme.yml with parent theme and groups config, plus assets.yml for SCSS registration",
        "SCSS files in 3-folder model (settings/variables/components) with color palette via sass:map merge",
        "Layout update YAML for product listing using @add or @addTree with @setBlockTheme",
        "Twig template for the warehouse availability badge"
      ],
      "files": []
    },
    {
      "id": 12,
      "eval_name": "oro-frontend-12",
      "skill": "oro-frontend",
      "prompt": "I need to create a JavaScript page component that adds a real-time stock counter to the product view page. When the page loads, it should fetch stock data from our custom API endpoint and update a DOM element. Use the Chaplin.js page component pattern that OroCommerce v6.1 uses.",
      "assertions": [
        "JS page component file following Chaplin/BaseComponent pattern with initialize and dispose methods",
        "Component registered in jsmodules.yml with correct module path",
        "Layout update YAML attaching component via data-page-component-module and data-page-component-options attributes"
      ],
      "files": []
    },
    {
      "id": 13,
      "eval_name": "oro-integration-13",
      "skill": "oro-integration",
      "prompt": "Set up an async message queue processor that handles warehouse stock sync. When a product's stock changes, a message should be sent to the 'acme.warehouse.stock_sync' topic. The processor should fetch the current stock from an external API, update the local WarehouseLocation capacity, and log the result. Include the producer service, processor, and all service registrations.",
      "assertions": [
        "Producer service using MessageProducerInterface to send messages to the topic",
        "Processor class implementing MessageProcessorInterface and TopicSubscriberInterface",
        "Processor returns ACK/REJECT/REQUEUE status constants",
        "Service registration with oro.message_queue.client.message_processor tag and topicName"
      ],
      "files": []
    },
    {
      "id": 14,
      "eval_name": "oro-integration-14",
      "skill": "oro-integration",
      "prompt": "Create an import processor for WarehouseLocation entities that can import from CSV. Include the data converter that maps CSV columns (location_name, location_code, max_capacity, active) to entity fields (name, code, capacity, isActive). Register it so it appears in the Oro import UI.",
      "assertions": [
        "Data converter class with convertToImportFormat and convertToExportFormat methods mapping CSV columns to entity fields",
        "Import processor/strategy class registered for the WarehouseLocation entity",
        "Service registration so the import appears in the Oro import/export UI"
      ],
      "files": []
    },
    {
      "id": 15,
      "eval_name": "oro-security-15",
      "skill": "oro-security",
      "prompt": "Set up complete ACL configuration for our WarehouseLocation entity. Define permissions for VIEW, CREATE, EDIT, DELETE in acls.yml. Add Acl attributes to the controller actions. Create a custom 'WAREHOUSE_INSPECT' permission that only applies to WarehouseLocation entities. Make sure the grid and API also respect these permissions.",
      "assertions": [
        "acls.yml with entity-type ACL definitions for CRUD operations",
        "PHP 8 #[Acl] attributes on controller action methods",
        "permissions.yml defining WAREHOUSE_INSPECT custom permission scoped to WarehouseLocation entity",
        "Notes on applying acl_resource in datagrid actions and API config"
      ],
      "files": []
    },
    {
      "id": 16,
      "eval_name": "oro-security-16",
      "skill": "oro-security",
      "prompt": "I have a custom repository method that fetches WarehouseLocations for a report. It's currently bypassing ACL \u2014 all users can see all locations regardless of ownership. Fix it so it respects the user's permission level using the ACL helper.",
      "assertions": [
        "Injection of AclHelper (oro_security.acl_helper) into the repository or calling service",
        "Usage of $aclHelper->apply($queryBuilder) on the custom query",
        "Explanation that ACL is not automatic on custom queries and must be explicitly applied"
      ],
      "files": []
    },
    {
      "id": 17,
      "eval_name": "behat-testing-01-suite-config",
      "skill": "oro-behat-testing",
      "prompt": "I'm setting up Behat for a new AcmeShippingBundle. I need three things in one go: (1) behat.yml that shows BOTH forms of suite registration \u2014 the auto-discovered short form AND the manual 'type: symfony_bundle' + 'bundle:' form \u2014 so I understand when to use which; (2) a shared_contexts block that inherits into every suite; (3) one element definition that maps a 'Shipping Method' form field to a nested sub-element using an xpath locator with element delegation (not a raw CSS selector). Walk me through each piece and why it's structured that way.",
      "assertions": [],
      "files": []
    },
    {
      "id": 18,
      "eval_name": "behat-testing-02-custom-context",
      "skill": "oro-behat-testing",
      "prompt": "Create a custom FeatureContext for OroInventoryBundle. It has to: extend OroFeatureContext, implement AppKernelAwareInterface and use AppKernelAwareTrait, receive @doctrine and a %acme.batch_size% parameter via constructor injection, and be registered in Tests/Behat/services.yml as public. Also register a ReferenceRepositoryInitializer that seeds a 'default_warehouse' reference so scenarios can use @default_warehouse. Show me the PHP class, the services.yml, and the initializer class with the correct Oro tag name.",
      "assertions": [],
      "files": []
    },
    {
      "id": 19,
      "eval_name": "behat-testing-03-feature-tag-mocking",
      "skill": "oro-behat-testing",
      "prompt": "Our checkout uses a PaymentClientInterface with three implementations \u2014 default, PayPal, Stripe. I want to swap the implementation per Behat scenario using feature tags (@use-paypal-mock, @use-stripe-mock). Give me: (1) the Tests/Behat/parameters.yml factory config using oro_test.behat.feature_tag_aware_factory with the argument array syntax, (2) a feature file showing the tag usage, (3) the exact bin/behat invocation including the flag and tag combination, and (4) explain what config_behat_test.yml does if anything in this flow.",
      "assertions": [],
      "files": []
    },
    {
      "id": 20,
      "eval_name": "behat-debug-01-missing-step",
      "skill": "oro-behat-debugging",
      "prompt": "I'm writing a Behat feature for OroCustomerBundle login flow and my step 'And I log in as customer user' fails because no definition matches. Before I write my own step, I want to discover: (a) all existing steps for OroCustomerBundle, (b) any step with 'login' in the description, (c) any step for flash messages in OroUserBundle. Give me the exact commands, then show me how to generate a skeleton for a genuinely new step with regex patterns if nothing matches. Also \u2014 I shouldn't have to re-run behat just to grep error messages; tell me the correct pattern for capturing full output to a file on first run.",
      "assertions": [],
      "files": []
    },
    {
      "id": 21,
      "eval_name": "behat-debug-02-xdebug-split",
      "skill": "oro-behat-debugging",
      "prompt": "I need to debug a failing checkout Behat test end-to-end. The Behat CLI runner is in one container, the PHP-FPM app under test is in a different container, and I want breakpoints in BOTH \u2014 one in the custom FeatureContext PHP class, one in the CheckoutController::submitAction. Walk me through: (1) enabling Xdebug on the CLI Behat process (env vars), (2) enabling Xdebug on PHP-FPM, (3) setting the XDEBUG_SESSION cookie in the Mink browser session so FPM picks up the debug session, (4) which IDE listener handles which process. Explain why these are two different debug targets.",
      "assertions": [],
      "files": []
    },
    {
      "id": 22,
      "eval_name": "behat-debug-03-ajax-flake",
      "skill": "oro-behat-debugging",
      "prompt": "My Behat test intermittently fails: after clicking 'Add to Cart', the next step 'I should see \"Product added\"' fails with element-not-found about 1 in 5 runs. There's already a waitForAjax in the custom element. Walk me through root-cause diagnosis \u2014 verbosity flags, screenshot inspection, how to capture the page state at failure, and the three most likely real causes (not 'add more sleep'). Give me one fix that's more principled than increasing timeouts.",
      "assertions": [],
      "files": []
    },
    {
      "id": 23,
      "eval_name": "functional-01-acl-controller",
      "skill": "oro-functional-testing",
      "prompt": "Write a PHPUnit functional test WarehouseLocationControllerTest extending Oro's WebTestCase. It must cover: (1) admin GET /admin/warehouse-location returns 200, (2) a limited user (loaded via built-in LoadUser fixture) GET returns 403, (3) admin POST create persists a WarehouseLocation and the response redirect ends on the view page, (4) GET /api/warehouselocations with an invalid API auth header returns 403 JSON. Use @dbIsolationPerTest at class level, the right assertion helpers (assertHtmlResponseStatusCodeEquals / assertJsonResponseStatusCodeEquals), and the built-in LoadUser + LoadOrganization fixtures. Explain why the class also needs @outputBuffering enabled.",
      "assertions": [],
      "files": []
    },
    {
      "id": 24,
      "eval_name": "functional-02-test-env-install",
      "skill": "oro-functional-testing",
      "prompt": "I'm setting up the functional test environment for a fresh OroCommerce checkout project. Give me: (1) the exact contents of .env-app.test.local with ORO_DB_DSN and ORO_MAILER_DSN, (2) the FULL oro_test_framework.install_options block in config/config.yml with every documented field (user_name, user_email, user_firstname, user_lastname, user_password, sample_data, organization_name, application_url, skip_translations, timeout, language, formatting_code, no_interaction \u2014 don't leave any out), and (3) the oro:install --env=test invocation. Most importantly: explain the gotcha about CLI --user-email=... flags not working in the test env, and why the install_options YAML is the only way to customize it.",
      "assertions": [],
      "files": []
    },
    {
      "id": 25,
      "eval_name": "e2e-01-remote-staging",
      "skill": "oro-e2e-testing",
      "prompt": "Set up end-to-end Behat tests against our staging at https://staging.example.com/. I need: (1) a behat.yml that imports behat.yml.dist, overrides base_url, and configures the artifacts handler with auto_clear: false; (2) a .behat-secrets.yml with login.username and login.password under a secrets: root; (3) a feature file that logs in via <Secret:login.username> / <Secret:login.password> and creates a shopping list through menu navigation (not direct URLs); (4) the exact bin/behat --skip-isolators invocation; (5) what to put in .gitignore and why committing the secrets file is a data breach. Also explicitly tell me the canonical way to run chromedriver for this setup.",
      "assertions": [],
      "files": []
    },
    {
      "id": 26,
      "eval_name": "e2e-02-fixture-env-file",
      "skill": "oro-e2e-testing",
      "prompt": "I want to run e2e tests against staging but pre-load an Alice fixture into the remote database before the scenarios. Show me: (1) the --skip-isolators-but-load-fixtures invocation, (2) which env file holds the ORO_DB_DSN for fixture loading \u2014 and reconcile the apparent contradiction in Oro's own docs where the e2e page says .app-env.local and the functional page says .env-app.test.local (which is correct for this context?), (3) the ORO_DB_DSN format for a remote Postgres, (4) the silent-failure gotcha that happens when local source code doesn't match the deployed version's migrations.",
      "assertions": [],
      "files": []
    },
    {
      "id": 27,
      "eval_name": "k6-01-full-pipeline",
      "skill": "oro-k6-testing",
      "prompt": "I want to benchmark our storefront from a local machine against https://staging.example.com/ using Amanda Cole's test account. Give me all three commands in the correct run order: (a) warm-up with 1 VU / 60s / THRESHOLD_95=3000, (b) storefront load test with 50 VU / 10m / THRESHOLD_95=850, (c) checkout flow against shopping list ID 42 with SHIPPING_METHOD=fixed_product_5 and PAYMENT_METHOD=payment_term_1. For each one, give me BOTH the native k6 invocation AND the grafana/k6 Docker equivalent (with the correct --network, volume mount, and user flags so it can write summary.html without permission issues). Also tell me the exact directory that summary.html lands in.",
      "assertions": [],
      "files": []
    },
    {
      "id": 28,
      "eval_name": "k6-02-custom-metric-threshold",
      "skill": "oro-k6-testing",
      "prompt": "I only care about the product listing page load time for logged-in users. I don't want to alert on home page, login page, or product detail page. Tell me: (a) the exact name of the custom Oro metric that measures logged-in product listing page loads (it's one of the 14 named custom metrics), (b) how to set a k6 threshold specifically on that metric without affecting others, (c) the list of all 14 custom Oro metrics so I know what else I could target, (d) the rationale for why warm-up uses THRESHOLD_95=3000 but load tests use 850.",
      "assertions": [],
      "files": []
    },
    {
      "id": 29,
      "eval_name": "ci-01-gitlab-pipeline",
      "skill": "oro-behat-ci",
      "prompt": "Write a GitLab CI job named test:behat for an Oro 6.1 project. It needs to: (1) bring up the full Oro test stack via docker compose (app, postgres, redis, elasticsearch/opensearch, message queue, chrome) \u2014 acknowledge that GitLab's native 'services:' keyword can't do this because of Oro's service mesh fanout, (2) run behat with the canonical formatter combo -f pretty -o std -f junit -o var/behat/junit --strict, (3) collect JUnit XML via artifacts:reports:junit AND screenshots from var/behat/ on failure, (4) use 3 parallel shards via GitLab parallel: keyword AND explain the interaction with --consumers=N (hint: 3 shards \u00d7 2 consumers each = 6 MQ workers contending), (5) include the Chrome headless mandatory flag set and note that the Chrome extension must come from the PHP container's vendor tree. Give me the .gitlab-ci.yml stanza.",
      "assertions": [],
      "files": []
    },
    {
      "id": 30,
      "eval_name": "ci-02-hang-on-cleanup",
      "skill": "oro-behat-ci",
      "prompt": "Our Oro Behat GitLab CI job runs all scenarios successfully (all green) but then hangs for 10+ minutes at the end until the runner kills it with a timeout. We're NOT on Jenkins. Root-cause walkthrough: think about consumer cleanup, the interaction between --consumers=N in-process workers and the long-running consumer container, isolator teardown, MessageQueue state that leaks by design (because there's no MessageQueueIsolator), and what oroinc/platform#954 has to say about this scenario. Give me three concrete things to try in order of likelihood.",
      "assertions": [],
      "files": []
    }
  ]
}
